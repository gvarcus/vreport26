# ========================
# CONFIGURACIÓN DE ENTORNO - ARCHIVO DE EJEMPLO
# ========================
# Este archivo muestra QUÉ variables se necesitan.
# COPIAR este archivo a .env y completar con valores REALES
#
# IMPORTANTE: Este archivo (.env.example) SÍ se commitea a Git
#             El archivo .env NO se commitea (está en .gitignore)

# ========================
# ODOO CONFIGURATION
# ========================
# URL del servidor Odoo (REQUERIDO)
ODOO_URL=https://tu-servidor-odoo.com

# Nombre de la base de datos Odoo (REQUERIDO)
ODOO_DB=nombre_base_datos

# ========================
# PostgreSQL DATABASE
# ========================
# Host del servidor PostgreSQL (REQUERIDO si se usa base de datos local)
DB_HOST=localhost

# Puerto de PostgreSQL (por defecto: 5432)
DB_PORT=5432

# Nombre de la base de datos (REQUERIDO si se usa base de datos local)
DB_NAME=vreportes

# Usuario de base de datos (REQUERIDO si se usa base de datos local)
DB_USER=postgres

# Contraseña de base de datos (REQUERIDO si se usa base de datos local)
DB_PASSWORD=tu_contraseña_muy_segura

# ========================
# TEST CREDENTIALS
# ========================
# Usuario de prueba para /api/test-odoo (REQUERIDO)
# Utiliza credenciales de PRUEBA, NO las de producción
TEST_USER=usuario_prueba@ejemplo.com

# Contraseña de prueba (REQUERIDO)
# Utiliza una contraseña de PRUEBA, NO la de producción
TEST_PASSWORD=contraseña_prueba_segura_123

# ========================
# SESSION & AUTHENTICATION
# ========================
# Secret para firmar sesiones (generar con: openssl rand -base64 32)
SESSION_SECRET=generar-un-string-aleatorio-largo-y-seguro-aqui

# Secret para firmar JWT tokens
JWT_SECRET=generar-otro-string-aleatorio-largo-y-seguro-aqui

# ========================
# SERVER CONFIGURATION
# ========================
# Entorno (development, staging, production)
NODE_ENV=development

# Puerto del servidor
PORT=8080

# ========================
# RATE LIMITING (OPCIONAL)
# ========================
# Máximo de intentos de login por IP
LOGIN_RATE_LIMIT_ATTEMPTS=5

# Ventana de tiempo para rate limiting (en minutos)
LOGIN_RATE_LIMIT_WINDOW_MS=900000

# ========================
# LOGGING (OPCIONAL)
# ========================
# Nivel de log (debug, info, warn, error)
LOG_LEVEL=info

# Guardar logs en archivo
LOG_TO_FILE=false

# ========================
# CORS CONFIGURATION (OPCIONAL)
# ========================
# Origen permitido para CORS (puede ser múltiple, separado por comas)
CORS_ORIGIN=http://localhost:3000,http://localhost:8080

# ========================
# INSTRUCCIONES
# ========================
# 1. Copiar este archivo:
#    cp .env.example .env
#
# 2. Editar .env con tus valores REALES:
#    nano .env
#
# 3. NUNCA commitear .env a Git:
#    - Ya está en .gitignore
#    - Solo comitea .env.example
#
# 4. Para generar secrets seguros:
#    openssl rand -base64 32
#
# 5. Proteger archivo .env:
#    chmod 600 .env
